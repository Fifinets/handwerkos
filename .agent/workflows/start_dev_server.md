---
description: Start the development server on port 8080
---

This workflow starts the Vite development server on port 8080 and exposes it to the network (host).

1. Start the server
// turbo
```bash
npm run dev -- --port 8080 --host
```
